"""Central place for project-wide settings: model names, chunk sizes,
default top-k, paths to data/corpus and data/eval, API key env var names.

Nothing here yet — fill in once the first pattern is actually built.
"""
