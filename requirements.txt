# Core app
streamlit>=1.22.0
pandas>=1.5.0
numpy>=1.23.0
requests>=2.28.0
python-dotenv>=1.0.0

# Orchestration
langchain>=0.3.0
langgraph>=0.2.0
langchain-community>=0.3.0

# GitHub ingestion
PyGithub>=2.3.0

# Chunking / doc loading
langchain-text-splitters>=0.2.0

# Embeddings — kept for a future swap back to Nebius (OpenAI-compatible);
# not on the active path while EMBEDDING_MODEL is a local sentence-transformers
# model (see app.common.config).
openai>=1.0.0

# Generation — Claude, via the official Anthropic SDK
anthropic>=0.40.0

# Vector store
chromadb>=0.5.0

# Sparse / keyword retrieval (hybrid RAG)
rank-bm25>=0.2.2

# Reranking
sentence-transformers>=2.2.0   # cross-encoder reranker

# Graph store — using networkx (in-memory, no server) for now; graph_build.py's
# GraphStore(backend="neo4j") is a documented TODO if that's swapped in later.
networkx>=3.2
# neo4j>=5.20.0                 # server-backed alternative, not implemented yet

# Evaluation
ragas>=0.1.0
